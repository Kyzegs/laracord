# Laracord documentation

VitePress site with browser-local full-text search powered by MiniSearch.

```bash
npm install
npm run dev
npm run build
```

Documentation lives in `docs/`. Site config and theme live in `docs/.vitepress/`.

Regenerate endpoint reference from package catalog:

```bash
php generate-api-docs.php
```
